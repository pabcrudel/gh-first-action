name: first workflow
on: workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Ver directorios
        run: ls
      # - name: Print Hello
        # run: echo "Hello World Action"
        # run: bash scripts/helloworld.sh
      - name: Print bye
        run: echo "bye bye"
